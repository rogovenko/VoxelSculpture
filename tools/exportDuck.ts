/**
 * Утка больше не собирается кодом: источник — `assets/vox/duck.vox`.
 * Эта команда оставлена, чтобы случайно не затереть правки из MagicaVoxel.
 */
console.error(
  'duck.vox is authored in MagicaVoxel now. export:duck would overwrite it and is disabled.',
);
process.exit(1);
