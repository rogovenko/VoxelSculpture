/**
 * Птенец больше не собирается кодом: источник — `assets/vox/chick.vox`.
 * Эта команда оставлена, чтобы случайно не затереть правки из MagicaVoxel.
 */
console.error(
  'chick.vox is authored in MagicaVoxel now. export:duck would overwrite it and is disabled.',
);
process.exit(1);
